using UnityEngine;

public class Ship_Manager : MonoBehaviour
{
    public void Start()
    {
        Destroy(gameObject, 2.5f);
    }


    private void OnMouseDown()
    {
        Debug.Log("Hit");
        Destroy(gameObject);
    }
}
